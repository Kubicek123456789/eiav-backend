# 🗄️ Database connection (Neon or Supabase)
DATABASE_URL=postgresql://your-username:your-password@your-db-hostname/db-name

# 🔐 JWT secret (used to sign login tokens)
JWT_SECRET=eiav_secret_key

# ☁️ AWS S3 storage (optional — skip if not using)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-access-key
S3_BUCKET=eiav-media

# 🌐 App port (keep as-is)
PORT=3001
